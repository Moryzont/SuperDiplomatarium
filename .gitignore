_site/
.sass-cache/
.jekyll-cache/
.jekyll-metadata
# Ignore folders generated by Bundler
.bundle/
vendor/

# Test output
tests/test-results.json
tests/temp-*

# Optional: Exclude legacy data to reduce repo size
# Uncomment these lines to deploy ONLY optimized data (~95MB instead of ~230MB)
# data/optimized/letters-light-*.json
# data/optimized/fulltext/

# Note: Original chunks (data/chunks/) are kept for backwards compatibility
# and to regenerate optimized data. If you only use search-v2.js, you can
# also exclude them by uncommenting:
# data/chunks/

# Search v3 generated artifacts (rebuild with: node scripts/build-search-v3.mjs)
data/v3/
node_modules/
